# گسترش رادار رقبا از ۱۱ به ۳۳+ اقامتگاه (همه اتاق‌های داشبورد تخمین درآمد)

> **Goal:** همه ۳۳ اقامتگاه داشبورد تخمین درآمد در رادار رقبا رصد شوند (تقویم روزانه + تاریخچه + گزارش تلگرام).

**وضعیت فعلی (بررسی‌شده):**
- `data/radar/radar-config.json` = ۱۱ اتاق (منبع واحد اتاق‌های رادار — `radar_common.py` از همین می‌خواند)
- `data/revenue/seydkola-mordad-1405.json` = ۳۳ اتاق (۹ اتاق مشترک با رادار → ۲۴ اتاق جدید)
- ۲ اتاق رادار (3240255 ابراهیم، 3169804 سیدابراهیم) در لیست ۳۳ نیستند → پس از افزودن، کل = ۳۵
- متادیتا (روستا/میزبان) هر ۲۴ اتاق جدید از دیتاست‌های محلی موجود است (همه سیدکلا)

**معماری:** اتاق‌ها فقط از کانفیگ رادار خوانده می‌شوند؛ همه اسکریپت‌ها (`fetch_radar`، `radar_history`، `build_radar_dashboard`، `radar_tg`، `radar_daily` لانچر کرون) خودکار اتاق‌های جدید را می‌گیرند → فقط کانفیگ + برچسب‌ها تغییر می‌کند، کرون (هر ۶ ساعت) بدون تغییر می‌ماند.

---

## گام‌ها

### Task 1: ساخت اسکریپت افزودن گروهی (بدون دست‌زدن به کد موجود)
- Create: `scripts/radar_bulk_add.py`
- از `data/revenue/seydkola-mordad-1405.json` می‌خواند، اتاق‌هایی که در کانفیگ نیستند را با متادیتای دیتاست‌های محلی + برچسب کوتاه پیشنهادی اضافه می‌کند
- برچسب کوتاه: نام میزبان + (در صورت تکراری‌بودن میزبان) نوع اقامتگاه — جدول پیشنهادی در پایین
- `--dry-run` برای پیش‌نمایش بدون ذخیره
- Verify: `python scripts/radar_bulk_add.py --dry-run` → ۲۴ اتاق، همه با متا

### Task 2: اعمال افزودن + فچ تقویم همه اتاق‌ها
- Run: `python scripts/radar_bulk_add.py` سپس `python scripts/fetch_radar.py`
- Verify: ۳۵ خط `[ok]` — هر اتاق ~۱۰۶ شب

### Task 3: سید تاریخچه SQLite
- Run: `python scripts/radar_history.py`
- Verify: `SELECT COUNT(DISTINCT room_id) FROM days` در `data/radar/history/radar_history.db` = ۳۵
- توجه: تاریخچه ۲۴ رقیب جدید از امروز شروع می‌شود (قابل backfill نیست — اسنپ‌شات گذشته وجود ندارد)

### Task 4: بازسازی داشبورد + بررسی رندر
- Run: `python scripts/build_radar_dashboard.py`
- Verify: `competitor-radar.html` ۳۵ ردیف اتاق دارد، خودی اول + مدال‌ها درست

### Task 5: رفع هاردکدها در گزارش تلگرام
- Modify: `scripts/radar_tg.py:227` — `{3297585}` → `OWN_IDS` از `radar_common`
- Modify: هر عدد ثابت «۳۳ اقامتگاه» در متن گزارش → `len(rooms)` داینامیک
- Verify: `python scripts/radar_tg.py` → خروجی بدون خطا، شمارنده‌ها با ۳۵ هماهنگ

### Task 6: تست کامل پایپ‌لاین (اجرای دستی لانچر)
- Run: `touch data/radar/.manual_run && cronjob run (Jajiga Dashboards Refresh)`
- Verify: گزارش تلگرام می‌رسد — گرید ۳۵ اتاق، تغییرات، درآمد ۳۳ اقامتگاه

---

## برچسب‌های کوتاه پیشنهادی (۲۴ اتاق جدید — قابل تغییر)
| ID | میزبان | برچسب پیشنهادی |
|---|---|---|
| 3219516 | عاطفه | عاطفه ویلا |
| 3206720 | عاطفه | عاطفه چوبی |
| 3178910 | محمد | محمد ویلا |
| 3190552 | صدیقه | صدیقه |
| 3253520 | کاظم | کاظم سوئیسی |
| 3299045 | حبیب | حبیب ویلا |
| 3227770 | محمد | محمد خانه |
| 3184142 | کاظم | کاظم چوبی |
| 3190700 | لیلا | لیلا چوبی |
| 3206573 | ابوالفضل | ابوالفضل |
| 3198301 | محمدرضا | م.ر سوئدی |
| 3195220 | محمدرضا | م.ر سنتی |
| 3198234 | محمدرضا | م.ر سوئدی۲ |
| 3195933 | محمدرضا | م.ر سوئیسی |
| 3179404 | محمدرضا | م.ر چوبی |
| 3149747 | میراحسان | میراحسان |
| 3201318 | فائزه | فائزه |
| 3237859 | حبیب | حبیب سوئیسی |
| 3149749 | میراحسان | میراحسان سوئیت |
| 3232816 | محمدرضا | م.ر خانه |
| 3262853 | فائزه | فائزه خانه |
| 3193646 | امیرحسین | امیرحسین |
| 3230216 | حسین | حسین ویلا |
| 3302232 | بهنام | بهنام |

## ریسک‌ها / نکات
1. **طول گزارش تلگرام** ~۳ برابر می‌شود (۳۵ اتاق) — طبق سلیقه کاربر (گزارش کامل، بدون حذف) مشکلی نیست
2. **برچسب‌های تکراری میزبان** (محمدرضا ۶ اتاق!) — پیشنهاد بالا با نوع اقامتگاه جدا شده؛ قبل از اجرا تأیید شود
3. **۲ اتاق اضافه** (3240255، 3169804) — پیشنهاد: نگه‌داشتن (کل ۳۵) تا تاریخچه از دست نرود؛ در صورت تمایل حذف می‌شوند
4. **کرون بدون تغییر** — لانچر هر ۶ ساعت همان اسکریپت‌ها را با کانفیگ جدید می‌زند

## خروجی
- `scripts/radar_bulk_add.py` (جدید)
- `scripts/radar_tg.py` (۲ اصلاح کوچک)
- `data/radar/radar-config.json` (۲۴ اتاق جدید)
- `data/radar/{id}.json` × ۲۴ + اسنپ‌شات امروز ۳۵ اتاق
- `data/radar/history/radar_history.db` (۳۵ اتاق)
- `competitor-radar.html` (۳۵ ردیف)
