# Jajiga Tracker — گزارش وضعیت پروژه
**تاریخ:** 2026-07-31

---

## ۱. خلاصه اجرایی

پروژه ردیابی قیمت کلبه‌های بابلکنار از سایت جاجیگا. شامل ۴ روستا و ۸۰ لیست فعال.
صاحب پروژه اپراتور کلبه Swiss Chalet در روستای سیدکلا است.

**وضعیت کلی: ✅ فعال و در حال کار**

---

## ۲. ساختار پروژه

```
jajiga-tracker/
├── data/
│   └── all-cabins.json          # 43 KB — منبع اصلی داده‌ها (۸۰ لیست)
├── scripts/
│   ├── collector.py             # 467 خط — اسکریپت جمع‌آوری داده (Playwright)
│   └── rebuild_dashboard_data.py # 120 خط — بازسازی داده‌های داشبورد از JSON
├── dashboard.html               # 66 KB — داشبورد تک‌فایل HTML (RTL, فارسی)
├── server.py                    # 34 خط — سرور HTTP ساده پایتون (پورت 8080)
├── requirements.txt             # playwright
└── README.md                    # توضیحات پروژه (فارسی)
```

**نکته مهم:** پروژه Git ندارد. هیچ نسخه‌ای از تغییرات ثبت نمی‌شود.

---

## ۳. داده‌ها (all-cabins.json)

### ۳.۱ خلاصه آماری

| روستا | تعداد لیست | بازه قیمت (تومان/شب) |
|--------|-----------|---------------------|
| سیدکلا | ۲۲ | ۱,۱۲۰,۰۰۰ – ۵,۵۰۰,۰۰۰ |
| قرآن تالار | ۱۱ | ۱,۴۰۰,۰۰۰ – ۳,۲۰۰,۰۰۰ |
| گونه کلا | ۲۴ | ۱,۵۰۰,۰۰۰ – ۸,۵۰۰,۰۰۰ |
| شیردارکلا | ۲۳ | ۱,۳۰۰,۰۰۰ – ۹,۰۰۰,۰۰۰ |
| **مجموع** | **۸۰** | **۱,۳۰۰,۰۰۰ – ۹,۰۰۰,۰۰۰** |

### ۳.۲ لیست مالک

شناسه مالک کلبه در داده‌ها: **`id=3297585`** با فلگ `is_own_listing: true`
- عنوان: کلبه سوئیسی
- قیمت: ۲,۷۵۰,۰۰۰ تومان/شب
- روستا: سیدکلا
- امتیاز: ۵.۰ (۴ نظر)

### ۳.۳ فیلدهای هر رکورد

هر رکورد شامل فیلدهای زیر است:

| فیلد | توضیح | منبع |
|------|-------|------|
| `id` | شناسه جاجیگا | اسکریپت |
| `title` | عنوان لیست | اسکریپت |
| `price` | قیمت پایه (تومان/شب) | اسکریپت |
| `price_source` | منبع قیمت (مثلاً `api_min_price`) | اسکریپت |
| `rooms` | تعداد اتاق خواب | اسکریپت |
| `floor` | متراژ زیربنا (متر مربع) | اسکریپت |
| `guests` | حداکثر ظرفیت نفرات | اسکریپت |
| `rating` | امتیاز (از ۵) | اسکریپت |
| `reviews` | تعداد نظرات | اسکریپت |
| `url` | لینک صفحه جاجیگا | اسکریپت |
| `village` | نام روستا | اسکریپت |
| `is_own_listing` | آیا لیست مالک است؟ | دستی |
| `active` | آیا لیست فعال است؟ | دستی/خارجی |
| `host` | نام میزبان | خارجی |
| `success_books` | تعداد رزروهای موفق | خارجی |
| `standard_capacity` | ظرفیت استاندارد | اسکریپت |
| `extra_capacity` | ظرفیت اضافه | اسکریپت |
| `last_scrape_status` | وضعیت آخرین جمع‌آوری | اسکریپت |
| `last_scrape_attempt` | زمان آخرین جمع‌آوری | اسکریپت |

### ۳.۴ یکپارچگی داده

- ✅ بدون شناسه تکراری (۸۰ شناسه منحصربفرد از ۸۰ رکورد)
- ✅ تمام رکوردها وضعیت scrape دارند (`last_scrape_status`)
- ⚠️ **داده‌ها توسط یک فرآیند خارجی بین جلسات ویرایش شده‌اند** — تعداد لیست‌ها از ۴۰ به ۸۰ افزایش یافته، فیلدهای جدیدی مانند `active`، `host`، `success_books` اضافه شده، و `price_source` از `calendar_base` به `api_min_price` تغییر کرده است. این تغییرات خارج از scope کار من بوده است.

---

## ۴. اسکریپت جمع‌آوری (collector.py)

### ۴.۱ فناوری

- **Playwright** (headless Chromium) — ضروری است چون جاجیگا سایت CSR (Client-Side Rendered) است
- قیمت، امتیاز، تعداد نظرات و سایر فیلدها فقط بعد از رندر شدن React قابل دسترسی هستند
- `__NEXT_DATA__` فقط شامل `title` و `description` است — فیلدهای مالی و عملکردی در DOM رندر شده هستند

### ۴.۲ نحوه کار

1. فهرست شناسه‌ها را از `data/all-cabins.json` می‌خواند
2. برای هر شناسه، صفحه جاجیگا را با Playwright باز می‌کند
3. با `page.evaluate()` و regex از متن DOM استخراج می‌کند
4. با داده‌های موجود ترکیب می‌کند (Merge نه Overwrite)
5. تاخیر تصادفی ۲-۴ ثانیه بین درخواست‌ها

### ۴.۳ محافظت از فیلدها

اسکریپت از الگوی `SCRAPER_FIELDS` استفاده می‌کند:
- فیلدهایی که اسکریپت مدیریت می‌کند: `title`, `url`, `price`, `rating`, `reviews`, `guests`, `rooms`, `floor`, `standard_capacity`, `extra_capacity`, `last_scrape_status`, `last_scrape_attempt`
- **تمام فیلدهای دیگر حفظ می‌شوند** (شامل `is_own_listing`، `active`، `host`، و هر فیلد سفارشی آینده)

### ۴.۴ عملکرد

| مقدار | مقدار |
|-------|-------|
| زمان کل برای ۴۰ لیست | ~۵ دقیقه ۲۰ ثانیه |
| زمان متوسط هر لیست | ~۸ ثانیه |
| نرخ موفقیت آخرین اجرا | ۱۰۰٪ (۴۰/۴۰) |

### ۴.۵ باگ اصلاح شده: آستانه قیمت تقویم

regex اولیه قیمت تقویم، قیمت‌های ۲۰۰,۰۰۰ تومانی از بخش «اقامتگاه‌های مشابه» را به اشتباه به عنوان قیمت پایه شناسایی می‌کرد. آستانه از `≥100,000` به `≥1,000,000` افزایش یافت.

---

## ۵. داشبورد (dashboard.html)

- **تک‌فایل HTML** — بدون React/Vite/Tailwind
- CSS تاریک، RTL، فونت Vazirmatn
- قابل اجرا با باز کردن مستقیم فایل در مرورگر
- `server.py` آن را روی `localhost:8080` سرو می‌کند

### ۵.۱ امکانات

- کارت‌های آمار کل (تعداد لیست‌ها، بازه قیمت، میانگین امتیاز)
- خلاصه هر روستا (تعداد کلبه، بازه قیمت، میانگین امتیاز)
- جستجو و فیلتر بر اساس روستا
- مرتب‌سازی بر اساس قیمت / امتیاز / تعداد نظر
- نمودار نسبی قیمت برای هر لیست

### ۵.۲ نحوه به‌روزرسانی

```bash
python scripts/rebuild_dashboard_data.py
```

این اسکریپت فایل `all-cabins.json` را می‌خواند و بلوک `const ALL_CABINS = [...]` در HTML را بازسازی می‌کند. فیلدهای scrape metadata (`last_scrape_status`, `last_scrape_attempt`) از خروجی حذف می‌شوند چون UI از آنها استفاده نمی‌کند.

### ۵.۳ فیلدهای موجود در داشبورد

تمام فیلدهای JSON (شامل `is_own_listing`, `active`, `host`, `success_books`, `price_source`) به جز metadata اسکریپت.

---

## ۶. سرور (server.py)

- Python stdlib `http.server` روی پورت ۸۰۸۰
- CORS headers برای توسعه محلی
- ⚠️ در ویندوز (cp1252) ممکن است به دلیل emoji در `print()` کرش کند
- فقط فایل‌های استاتیک سرو می‌کند — API ندارد

---

## ۷. وابستگی‌ها

```
# requirements.txt
playwright>=1.40.0
```

ابزارهای نصب شده:
- Python 3.11.15
- Playwright 1.61.0 + Chromium
- pip

---

## ۸. مشکلات شناخته‌شده

| # | مشکل | شدت | وضعیت |
|---|-------|------|-------|
| ۱ | **Git وجود ندارد** — هیچ تاریخچه نسخه‌ای ثبت نمی‌شود | متوسط | حل‌نشده |
| ۲ | **داده‌ها توسط فرآیند خارجی ویرایش می‌شوند** — بین جلسات فیلدهای جدید اضافه و مقادیر تغییر می‌کنند بدون اینکه اسکریپت scraper اجرا شود | متوسط | نیاز به بررسی |
| ۳ | **`server.py` emoji crash** — در ویندوز cp1252 کار نمی‌کند | کم | حل‌نشده |
| ۴ | **README قدیمی** — ساختار پروژه و فازها در README با واقعیت مطابقت ندارد | کم | حل‌نشده |
| ۵ | **بدون test suite** — pytest هیچ تستی پیدا نمی‌کند | کم | حل‌نشده |
| ۶ | **بدون cron/برنامه‌ریزی** — جمع‌آوری باید دستی اجرا شود | متوسط | حل‌نشده |
| ۷ | **تقویم/دسترسی‌پذیری** — scraping تقویم رزرو اجرا نشده | متوسط | پیاده‌سازی نشده |
| ۸ | **محاسبه درآمد** — منطق درآمد رقبا وجود ندارد | متوسط | پیاده‌سازی نشده |

---

## ۹. تغییرات اخیر (Timeline)

| تاریخ | تغییر |
|-------|-------|
| ۳۰ تیر ۱۴۰۵ | ایجاد پروژه — فایل‌های اولیه (collector.py اولیه با urllib) |
| ۳۰ تیر ۱۴۰۵ | حذف شناسه تکراری 3240255 از JSON |
| ۳۰ تیر ۱۴۰۵ | ایجاد `rebuild_dashboard_data.py` — بازسازی داشبورد |
| ۳۰ تیر ۱۴۰۵ | ارتقاء collector.py از urllib به Playwright |
| ۳۰ تیر ۱۴۰۵ | اجرای کامل scraper روی ۴۰ لیست — ۱۰۰٪ موفق |
| ۳۰ تیر ۱۴۰۵ | اصلاح باگ آستانه قیمت تقویم (200k false positive) |
| ۳۰ تیر ۱۴۰۵ | اضافه کردن `is_own_listing: true` به لیست مالک |
| ۳۰ تیر ۱۴۰۵ | سخت‌سازی منطق merge (SCRAPER_FIELDS) |
| ۳۱ تیر ۱۴۰۵ | ویرایش خارجی داده‌ها — افزایش از ۴۰ به ۸۰ لیست، فیلدهای جدید |
| ۳۱ تیر ۱۴۰۵ | جابجایی `is_own_listing` از 3240255 به 3297585 |

---

## ۱۰. سؤالات باز برای بررسی

1. **چه فرآیندی داده‌ها را بین جلسات ویرایش می‌کند؟** — فایل JSON بین sessionها تغییر می‌کند بدون اجرای collector.py. آیا ابزار دیگری (ChatGPT, Claude, اسکریپت جداگانه) به این فایل دسترسی دارد؟

2. **آیا شناسه مالک واقعاً 3297585 است؟** — قبلاً 3240255 (کلبه استخردار) بود، حالا 3297585 (کلبه سوئیسی ساده‌تر). کدام درست است؟

3. **آیا فیلدهای `active`، `host`، `success_books` معتبر هستند؟** — این فیلدها توسط اسکریپت collector ما اضافه نشده‌اند. منبع آنها مشخص نیست.

4. **آیا قیمت‌های فعلی (`price_source: api_min_price`) دقیق‌تر از `calendar_base` هستند؟** — منبع قیمت از تقویم تغییر کرده بدون توضیح.

5. **آیا داشبورد با ۸۰ لیست جدید بازسازی شده؟** — آخرین rebuild قبل از ویرایش خارجی اجرا شد. نیاز به اجرای مجدد `rebuild_dashboard_data.py` است.
